# CartPilot · 把小红书的"种草"自动拼成"购物车"

> RFT · 小红书 Red Finance Trainee · Builder Test 48h Demo
> Live Demo: **[https://auracui01.github.io/CartPilot/](https://auracui01.github.io/CartPilot/)**

CartPilot 是一个面向小红书电商场景的 **Agentic Shopping Cart**。它基于用户在社区里的搜索、浏览与停留信号，自动在小红书商城中拼出一份符合预算和频率的购物车 —— 用户不再需要自己回忆看过什么、比品牌、凑单、控预算，只需要 **approve** 一下，就可以直接下单。

---

## 一、解决的问题

在内容社区里，用户的购买意图往往已经非常明确，但从"被种草"到"真正下单"之间仍然存在一段**高摩擦链路**：

- 用户要自己回忆看过哪些笔记、想买什么；
- 要在多个品牌之间比较；
- 要控制预算、凑齐购物车；
- 还要在"买/不买"之间反复犹豫。

结果是：**高购买意图没有被及时转化**，购物决策成本长期由用户自己承担。

CartPilot 把推荐系统从"给出若干单品"升级为"替用户完成一段从意图识别 → 选品组合 → 下单准备的购物决策"。

---

## 二、核心特性

- 🛒 **预算驱动组车**：用户设定推荐频率（每天 / 每周 / 每月）和预算上限（¥200 – ¥1200），CartPilot 在 30+ 商品池里自动选品组装，目标价位贴近预算上限。
- 🎯 **多模式拼车**：四档购买模式 —— 实用 / 品牌 / 颜值 / 性价比，每种模式下选品权重不同。
- 🔄 **三套购物车循环**：点"刷新购物车"在当前预算约束下循环出不同组合，不会卡死在同一套方案。
- 🧠 **可解释的信号面板**：展示 CartPilot 为什么这样拼 —— 中古/法式 88、敏感肌修护 81、品牌质感 74、通勤场景 63 等加权信号。
- 🔁 **逐件可替换**：每件商品下方横向滚动 6 个替代方案（同价位 / 更便宜 / 升级款 / 风格外扩 / 形态替代），点"换成这件"即时替换，预算条同步更新。
- ✅ **Approve-to-Buy**：保留用户最终下单权，把"选品 / 比价 / 凑单 / 替代决策"压缩成一次从上往下的确认。
- 📈 **商业预测模块**：内嵌 GMV 增长曲线（加入 CartPilot vs. 不加入），转化率和用户粘性的乐观假设。
- 📱 **可交互内嵌 demo**：营销页底部嵌入了一台手机大小的真机框，可直接体验从拼车到下单的完整流程。
- 🌗 **深色模式**：跟随系统主题，可手动切换。

---

## 三、Demo 操作指南

打开 [https://auracui01.github.io/CartPilot/](https://auracui01.github.io/CartPilot/) 后：

1. 上滑浏览 **痛点 / 功能 / 转化率与粘性预测 / GMV 增长曲线**。
2. 滚动到底部，会看到一台嵌入的手机界面。
3. 在手机内的**偏好条**调整：
   - 推荐频率：每天 / 每周 / 每月
   - 预算上限：拖动滑条（¥200 – ¥1200）
   - 购买模式：实用 / 品牌 / 颜值 / 性价比
4. **拖动预算** → CartPilot 实时重组购物车，slot 数会随预算从 3 件扩展到 5 件，可能命中通勤耳机这种 hero 单品。
5. 点 **更多选择** 中的"换成这件" → 替换单件并自动更新预算条。
6. 点 **刷新购物车** → 在当前预算约束下循环换出不同组合。
7. 点 **确认并下单** → 进入模拟下单成功页。

---

## 四、技术实现

- 纯 **静态 HTML / CSS / JavaScript**，无构建工具、无后端、无依赖。
- 单页结构 + 内嵌可交互手机模拟器。
- 商品池：30+ SKU 横跨配饰、护肤、通勤、家居、数码 5 类，价位 ¥68 – ¥1280。
- 组车算法：
  1. 按预算决定 slot 数（3 / 4 / 5）；
  2. 给每个 slot 计算目标价位（剩余预算 ÷ 剩余 slot 数，按 variant 偏移 ±25%）；
  3. 综合"价格接近度 + 模式权重 + variant 偏移"打分，从 top 3 中挑选；
  4. 完成后再做一轮 **预算回填** —— 若总价 < 92% 预算，自动升级最贵 slot；若超预算，则降级。
- 替代品生成：每件商品自动派生 6 个替代方案（同价位 / 更便宜 / 升级款 / 风格外扩 / 形态替代）。
- 暗色模式：CSS 变量 + `data-theme="dark"`，首次加载跟随 `prefers-color-scheme`。

---

## 五、文件结构

```
CartPilot/
├── index.html      # 营销页结构 + 内嵌手机 demo HTML
├── styles.css      # 全部样式（含 #phoneScope 内的 demo 样式 + 深色模式）
├── app.js          # 商品池 + 组车算法 + 全部交互逻辑
└── README.md       # 本文件
```

---

## 六、本地预览

**方式 A · VSCode Live Server（推荐）**
1. 用 VSCode 打开本仓库。
2. 安装扩展 `Live Server`（作者 Ritwick Dey）。
3. 右键 `index.html` → `Open with Live Server`。

**方式 B · 命令行**
```bash
python3 -m http.server 8000
# 浏览器打开 http://localhost:8000
```

---

## 七、部署到 GitHub Pages

本仓库已经通过 GitHub Pages 部署：

1. 仓库 Settings → Pages
2. Source: `Deploy from a branch`
3. Branch: `main` · Folder: `/ (root)`
4. Save，1–2 分钟后即可访问 `https://<用户名>.github.io/<仓库名>/`

每次 `git push` 到 `main` 后会自动重新部署，链接不变。

---

## 八、替换商品图（可选）

商品默认使用色块占位。如需上真实商品图：

在 `app.js` 的 `ITEM_POOL` 中给任意商品加一行 `image` 字段：

```js
{
  name: 'Maison Aurore 中古丝质领带',
  brand: 'Maison Aurore · 复古真丝',
  price: 168,
  image: './assets/maison-aurore-tie.jpg',   // ← 加这一行
  reason: '...',
  tags: ['法式','丝质','品牌质感']
}
```

有 `image` 的会自动覆盖色块，没加的保持原样。建议图片比例：

- 主车 thumb：5:6 竖图（约 288×344）
- 更多选择 swap-thumb：2:1 偏方（约 512×256）

---

## 九、产品思考摘要

CartPilot 的核心不是"再推荐一件商品"，而是：

- **从"逛"和"搜"中识别短期购买意图**（搜索词、停留时长、收藏、互动）；
- **在预算和频率约束下拼出一整车**，而不是单点推荐；
- **保留用户的最终选择权**（替换、删除、刷新、approve）；
- **压缩种草到下单之间的决策链路**。

这是把"购物决策"产品化，而不是再做一个推荐 feed。

---

## 十、致谢与声明

- 本项目为小红书 RFT Builder Test 48h Demo 提交作品。
- 所有商品、品牌、价格、推荐理由均为 demo 演示数据，**不代表任何真实品牌或商品**。
- 商业预测中的转化率与用户粘性使用了"偏乐观假设"，仅用于 demo 呈现，**不构成任何投资或经营建议**。

---

**Made with ❤️ by [@AuraCui01](https://github.com/AuraCui01) · 2026**
